<menu
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:material="http://schemas.android.com/apk/res-auto">
	<item
		android:id="@+id/new_decision"
		android:title="@string/new_decision"
		android:icon="@drawable/ic_action_add"
		material:showAsAction="ifRoom"/>
	<item
		android:id="@+id/decisions_list"
		android:title="@string/decisions_list"
		android:icon="@drawable/ic_action_list"
		material:showAsAction="ifRoom"/>
	<item
		android:id="@+id/sort_arguments"
		android:title="@string/sort_arguments"
		android:icon="@drawable/ic_action_sort"
		material:showAsAction="ifRoom"/>
	<item
		android:id="@+id/remove_decision"
		android:title="@string/remove_decision"
		android:icon="@drawable/ic_action_remove"
		android:visible="false"
		material:showAsAction="ifRoom"/>
</menu>
